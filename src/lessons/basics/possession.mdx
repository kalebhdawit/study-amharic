import PossessionTable from '../../components/PossessionTable.jsx'
import { PossessionExamples } from '../../components/translations/PossessionTranslations.jsx'

In order to show that a person possesses something, we can use the prefix የ- (ye-) before the pronoun or person's name. Let's look at ways to describe ownership of a house (ቤት).

<PossessionExamples />

Another commonly used way to show who possesses a noun is to add a pronoun-specific suffix. Here they are:

<PossessionTable />
